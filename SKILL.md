---
name: modelarts-vlm-throughput-tuning
description: 在华为云/HCSO ModelArts Standard 上为视觉语言模型设计和执行可回退的吞吐、延迟、并发、批处理、缓存、显存及上下文窗口调优。适用于不同模型、量化、卡数、Region 和图像理解/结构化输出项目；技能不携带历史项目参数或成绩。部署尚未可用时只做调优准备，不把启动成功当性能通过。
---

# ModelArts VLM 高吞吐调优

在不改变用户业务口径的前提下，用可回退的受控实验找到“已测配置中的最佳稳定候选”。模型、量化、框架、卡数、输入分布、输出策略、并发档位和目标值均来自当前项目，不继承任何历史项目。把启动配置、运行证据、原始请求记录和结论连成可追溯链；不把最高单次成绩宣称为全局最优。

## 先判断当前任务

区分整理历史、设计压测、准备脚本、实际改参、执行压测、分析结果和回退。用户只问“之前做了什么”或“写报告”不授权改变在线服务。用户说暂停或停止时，立即停止发新负载，不依据旧授权继续。

若模型、镜像、权重、ModelArts Standard 服务或测试入口尚未可用，只准备调优计划和测试器；明确缺少的前置条件，不把部署排障混入性能结论。

## 调优前强制问询

只要用户请求开始、继续或规划一个新项目的调优，先完整读取 [调优前问询清单](references/pre-tuning-checklist.md)。从本轮对话、项目文件和只读运行证据中回填已有答案，只询问尚未明确且会改变测试方案的项目；不要让用户重复回答已知信息。

在实际改参或发送正式负载前，必须向用户展示：

1. 已确认的业务场景和验收目标。
2. 仍未知或存在冲突的信息。
3. 展开后的候选参数和压测矩阵，包括总格数、重复次数、预计时长和停止条件。
4. 准备修改的服务、允许的停机范围及回退版本。

用户确认上述摘要后再执行。只要求方案、脚本或报告时，不因缺少在线权限而阻塞可完成的离线工作。

## 场景路由

先用用户的业务描述选择工作负载分支，不默认把 VLM 等同于 OCR 或证件识别：

| 场景 | 重点口径 | 处理方式 |
|---|---|---|
| 通用单图理解、分类、检测或 VQA | 分辨率分布、输出类型、准确率、请求吞吐 | 使用本文件的通用流程 |
| 多图对比、图文检索或多模态对话 | 每请求图片数、总视觉 token、轮次历史、复用率 | 使用通用流程，并按图片数和历史长度分层 |
| 视频理解 | 时长、帧采样、分辨率、音频、输出长度 | 将视频预处理和模型推理解耦计时 |
| 文档、票据、表单、证件 OCR/NER/JSON | 页数、版式、语言、手写、字段 schema、唯一文档缓存 | 额外读取 [文档与证件识别分支](references/scenario-document-ocr.md) |

场景不在表中时，按 work item、输入分布、输出策略、质量判定和流量模型重新定义，不强行套用证件识别参数。

## 冻结验收合同

开始新实验前，从用户最新要求和当前服务证据中确认并记录：

- 模型、量化、推理框架及实际镜像/运行版本。
- NPU 型号、卡数、TP/DP 拓扑；固定卡数未经用户解除不得扩容。
- 单请求输入构成、预处理方式，以及尺寸、时长、帧数、页数或视觉 token 分布。
- prompt、输出协议、thinking、temperature、输出上限、流式模式、工具调用与重试策略。
- 请求总数、并发档位、超时、冷热状态、缓存命中条件和网络路径。
- 吞吐、延迟和成功率目标；按当前接口单位选择 requests/s、images/s 或 tokens/s。若给出总量和时限，计算最低持续吞吐，并说明生产余量。
- 允许改变的参数、停止条件、最大测试时间和回退版本。

将合同写入实验 manifest。可使用 [实验记录模板](references/experiment-manifest.md)，不要继承旧账号、旧 Region、旧服务名、IP、密钥或路径。

## 建立可回退基线

实际改参前保存并核验：

- 当前 ModelArts 版本、启动参数、脚本 hash、镜像摘要和模型路径。
- 运行进程的真实 flags、模型接口报告的上下文上限、所有卡的可见性和健康状态。
- 旧版本或脚本备份及明确回退方式。
- 一轮符合验收合同的完整基线，而非仅 smoke、短样本或重复图片成绩。

每个候选记录父基线、唯一变化、假设、预期方向、验证方法、通过门槛和回退条件。不同框架的参数只能说明设计意图，不能直接做数值 A/B。

## 构造可信 VLM 压测

### 输入与输出

- 用当前业务的一个 work item 定义一次请求：它可以是单图、多图、视频、文档页、图文轮次或其他组合。
- 固定预处理管线和输入分布，包括解码、缩放/采样、编码、媒体传输方式、数量及 P50/P95/最大规模。
- 按真实复用率构造唯一、重复或混合数据；缓存命中条件必须单独记录。
- 固定 prompt、输出协议和质量判定器。结构化输出校验 schema；分类/检测校验标签或框；生成任务校验完整性、正确性和 finish reason。
- thinking、temperature、`max_tokens`、streaming 和工具调用属于验收合同，改变后建立独立结果序列。

### 客户端与统计

- 使用线程或异步客户端的持久连接；每个 worker 复用连接，避免每请求握手。
- 用单调时钟计算整轮 wall time。请求级任务报告 requests/s；含多个媒体对象时同时报告 work items/s；生成型任务同时报告 output tokens/s。
- 同时保留 expected、started、ended、success、failed、attempts、retried、HTTP 状态和错误类型。
- 延迟至少报告 P50、P95 和最大值，同时报告输入规模、prompt token、视觉 token 和 completion token 分布中当前可获得的指标。
- 失败等待和重试时间不能从整批 wall time 中剔除；成功子集延迟不能冒充整体 SLA。

## 冷启动、预热与正式轮次

服务重建后分别记录：

1. 小请求健康检查：只确认接口、后端和 JSON 功能。
2. cold warmup：允许触发懒编译，保留结果但不计入稳态容量。
3. immediate warmup：确认编译恢复及目标请求形状可运行。
4. 完整正式轮次：使用确认的请求数和主并发。
5. 同配置立即复测：判断高值是否可重复，并观察缓存填充、淘汰或尾延迟漂移。

短样本、warmup 和正式验收必须使用不同标签。小样本可筛选方向，但不能替代完整验收。

## 调参顺序

优先处理会改变真实业务计算量的请求侧旋钮，再进入服务端旋钮。每次尽量只改变一个主要变量。

### 请求侧

- 视觉输入规模：严格以客户确认的尺寸、页数、帧数、时长、图片数或视觉 token 分布为验收口径，更小输入只作探索。
- 输出上限：从业务真实输出分布设定；过小会截断 JSON，过大可能拖累调度。
- thinking 和采样：根据当前任务质量要求决定，不继承其他项目设置。
- 客户端并发：围绕目标向上和向下探测；吞吐上升但 P95 急剧恶化不算稳定收益。

### 服务端

- TP/DP：短请求高吞吐通常优先评估较小 TP、较大 DP，但先确认当前模型、量化和上下文是否能在较小 TP 下容纳；拓扑变化是独立版本。
- `max-num-seqs`：提高排队和批处理空间，观察实际每副本在途请求，避免只增加上限而客户端未利用。
- `max-num-batched-tokens`：围绕基线逐级试探；过小浪费聚合，过大可能加剧显存压力、decode 干扰和尾延迟。
- `gpu-memory-utilization`：只小步增加；以 KV 容量、OOM、EngineDead、P95/P99 和重试为约束，不能只看能否启动。
- 多模态处理器缓存：重复媒体可能受益；多数唯一图片可能因 LRU 填充和淘汰发生漂移。必须用真实缓存条件 A/B。
- prefix cache：只有共享前缀可验证且确实命中时才评估；不能把响应缓存或重复图片速度算作模型吞吐。
- async scheduling、CPU/OMP 线程、processor worker：一次只改一个，并关注 JSON 完整率、CPU 预处理和尾延迟。

## 上下文窗口搜索

上下文是容量与性能的联合验收，不是“服务能启动”即可。

- 先记录实际请求 token 分布，避免为不存在的 256K 业务牺牲高吞吐。
- 每个候选先做启动和 KV 容量校验，再完成相同输入、输出和并发的正式轮次。
- 若用户要求“失败则 128K、再失败 64K”或二分搜索，按用户给定上下界执行；每次保留独立 ModelArts 版本和已知稳定回退版本。
- 显存利用率只允许小步提高以满足 KV 校验；若启动成功但 QPS、P95 或成功率不达标，该窗口仍判为性能失败。
- 记录服务报告的实际 `max_model_len` 和进程 flags，不能只相信 UI 配置值。
- 客户端位置或网络路径变化时，结果标为方向性比较，除非重新跑同路径对照。

## 晋升、回退与停止

候选只有同时满足以下条件才可晋升：

- 符合全部客户输入和输出约束。
- 完整请求数和主并发口径一致。
- 使用真实缓存状态，且无 fallback 或错误后端污染。
- 功能质量不退化，当前任务的输出有效率和成功率达标。
- QPS 改善可复测，P95、最大延迟、重试和错误没有不可接受恶化。

出现 OOM、EngineDead、容量校验失败、响应不完整、重试激增、严重尾延迟、错误后端或旧请求未排空时，停止发新负载，保留失败记录并回退。连续多个单变量候选无收益或达到时间边界时，交付“已测配置中的最佳稳定候选”和剩余限制。

不得通过缩小客户输入、缩短真实输出、只算成功请求、改变真实数据复用率、隐藏失败、开启 fallback 或挑选一次峰值来达标。

## 报告要求

结束时输出：

- 初始原型与首个同框架可比基线，明确两者区别。
- 从基线到最终候选的参数变化和每项假设。
- 每轮配置、成功/尝试、吞吐、P50/P95/最大延迟、重试和结论。
- 所有超过目标但未采用的成绩及其不可比原因。
- 上下文专项测试的 KV 容量、性能变化和回退依据。
- 最终候选、已拒绝参数、服务当前运行态和复测清单。
- 明确“最好单次”“可比区间”“最终验证值”及是否具有合同安全余量；不得引用其他模型或 Region 的历史成绩作为当前承诺。

原始记录先落盘，再生成 Markdown、Word 或 Excel 等用户要求的交付物。报告不写 API key、密码、Cookie、OTP 或私钥。

## 方法参考

需要规划参数顺序、设计试探区间或解释为什么某个最高成绩不可采用时，读取 [通用调优策略](references/tuning-strategy.md)。该参考只包含决策方法和变量关系，不包含任何历史模型、固定卡数、固定尺寸或项目成绩。
