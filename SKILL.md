---
name: model-inference-testing
description: 为不同模型、接口、推理框架、硬件和业务场景设计、执行及复核可复现的功能、性能、容量、稳定性、质量与回归测试。开始前必须问清测试目的、请求语义、指标公式、测试矩阵、基线和通过标准；默认只测试和采集证据，不擅自调参、重启、扩容或用 Fallback 隐藏失败。GLM、Qwen 和特定 Agent/多模态协议作为独立分支。
---

# 模型推理测试

用冻结的请求、环境和指标口径回答“这个版本在这个场景下是否通过”。测试 Skill 不负责寻找最佳参数，也不把不同模型、路径、缓存或客户端的数据直接当成可比结果。

## 第一步：先问清楚，再行动

收到新测试、Benchmark、压测、容量验证或性能对比请求时，先完整读取 [测试前问询清单](references/pre-test-checklist.md)。

1. 预填对话、文件和只读证据中的答案，请用户确认。
2. 在被测对象、真实负载、指标公式、矩阵、质量标准和安全上限不清楚时，不直接启动正式测试。
3. 用户不知道的运行版本、实际参数和监控能力可通过只读检查补齐；未知项必须写入风险。
4. 发送正式负载、使用付费资源、读取敏感数据或进行破坏性/故障测试前，展示完整测试合同并取得授权。
5. 默认不修改模型、网关、Agent 或部署配置。若为测试变体必须修改，将每个变体视为独立被测版本，并要求变更及回退授权。

## 明确测试类型

- **功能测试**：接口、Streaming、Reasoning、Tools、JSON、Embedding、多模态等能力。
- **质量测试**：任务正确率、Recall/Precision、Schema、Judge、人评或媒体质量。
- **性能测试**：延迟、吞吐和资源效率。
- **容量测试**：稳定并发/QPS、最大输入/上下文、队列和恢复。
- **稳定性测试**：持续负载、错误率、内存/缓存漂移、重启和恢复。
- **回归测试**：与明确的历史版本或候选版本同条件比较。
- **韧性测试**：实例、网络、上游或依赖异常；只有用户明确授权才执行故障注入。

一个项目可以包含多种测试，但要分别定义通过标准。

## 冻结测试合同

使用 [测试计划与基线模板](references/test-plan.md) 记录并确认：

- 被测模型/服务/版本、实际工件和运行环境；
- 客户端到模型的完整路径与对照路径；
- 请求协议、Prompt、采样、Streaming、Reasoning、Tools 和结束语义；
- 输入、输出、并发/QPS、Arrival、缓存和数据复用分布；
- 全量矩阵、请求数、重复、随机种子、超时和最长时间；
- 指标公式、采集点、聚合方法及通过阈值；
- 质量判定、测试数据权限和隐私处理；
- 最大负载、费用、停止条件和恢复要求。

多维需求展开为完整笛卡尔积并说明总格数。若先做筛选，应明确“筛选矩阵”和“正式矩阵”，由用户批准，不能把筛选结果作为完整验收。

## 建立可比基线

测试开始前确认：

- 模型 Revision、量化/精度、Tokenizer/Processor、模板和 Adapter；
- 镜像/框架/驱动/工具链及实际进程参数；
- 硬件、节点、副本、拓扑和资源是否独占；
- 网关、鉴权、网络、客户端和测试机；
- 服务空闲/背景流量、缓存状态、预热/编译状态；
- 健康、队列、重启、错误和时钟同步。

对比测试除被比较变量外必须保持一致。做不到时标为“方向性对比”，列出混杂因素，不输出精确提升百分比。

优先同时保留最短模型路径与真实业务路径；前者定位模型，后者反映实际用户体验。不得把两条路径的结果混成同一列。

## 选择分支与指标

按实际业务读取 [测试场景分支](references/workload-branches.md)。GLM 或 Qwen 读取 [GLM/Qwen 测试分支](references/branches/glm-qwen.md)。

通用统计规则：

- 报告请求数、Started、Completed、Success、Failed、Timeout、Retried、空/无效响应。
- 延迟至少报告 P50/P95/P99/最大值和完整分布或原始明细。
- 系统吞吐以整轮 Wall Time 计算，包含失败、排队和重试。
- Per-request 生成速率与 Aggregate Throughput 分开。
- Streaming Reasoning 模型的 TTFT 包含首个 `reasoning_content` 或等价模型增量；需要时另报首个 Content Token。
- 非 Token 模型使用业务单位，不强行报告 TTFT/TPOT。
- HTTP 200 但无有效内容、Reasoning、Tool Call、Embedding 或媒体输出，按失败处理。

## 执行顺序

1. **连通性与版本快照**：不计入成绩。
2. **功能冒烟**：验证协议、字段、结束事件和目标能力。
3. **冷启动/首次编译**：单独记录。
4. **预热**：达到预先定义的稳态，不丢弃异常但与正式轮次分开。
5. **正式矩阵**：固定顺序或随机化顺序，记录背景流量和资源。
6. **重复轮次**：验证重要差异和尾延迟。
7. **容量/持续压力**：逐级升载，达到停止条件立即停止新请求。
8. **恢复与压力后功能回归**：确认队列、内存、实例和功能恢复。

不要高频轮询长测试。保存 PID、日志、断点、Manifest 和已完成 Cell，使任务可续跑。用户要求暂停时立即停止新 Cell，并保存当前结果。

## 数据质量检查

结果进入报告前检查：

- 请求数与明细行数一致；
- Token/媒体计数来源明确；
- 时间戳使用单调时钟，跨机分析确认时钟同步；
- 失败、取消、重试和 Fallback 没有被隐藏；
- 缓存命中和背景流量与标签一致；
- 没有输出截断、空流、错误模型、错误 Endpoint 或错误数据集；
- 平均值没有替代尾延迟，峰值没有冒充持续能力；
- 正确率计算包含固定样本集和可复核判定。

发现口径或数据问题时，标记该 Cell 无效并重测；不要修饰数字使其通过。

## 停止条件

达到用户确认的任一条件时停止新增负载：

- OOM、进程/实例死亡或反复重启；
- 通信、Kernel、图编译或数据损坏错误；
- 错误率、超时、尾延迟、队列或资源超过安全线；
- 输出质量或功能失效；
- 达到时间、费用、请求或负载上限；
- 服务在规定时间内无法恢复。

测试失败不是调优授权。保留证据并报告，不擅自修改生产配置。

## 交付内容

- 问询答案、未决项和测试合同；
- 环境/版本/实际配置基线；
- 完整矩阵及每个 Cell 状态；
- 原始请求级数据、日志和资源指标位置；
- 性能、容量、稳定性、质量和功能结果；
- 无效数据、失败、限制和混杂因素；
- 通过/不通过结论及证据；
- 可复现命令、续跑方法和后续建议。

报告中统一单位、公式和语言，不包含密码、API Key、Cookie、OTP、私钥或敏感请求正文。
